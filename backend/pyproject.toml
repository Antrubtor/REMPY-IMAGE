[project]
name = "rempy-backend"
version = "0.1.0"
description = "Backend FastAPI pour REMPY-IMAGE"
requires-python = "==3.12.*"
dependencies = [
    "fastapi[standard]==0.129.0",
    "sqlalchemy[asyncio]>=2.0.0",
    "asyncpg>=0.30.0",
]
